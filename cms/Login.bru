meta {
  name: Login
  type: http
  seq: 1
}

post {
  url: {{LoginUrl}}
  body: json
  auth: inherit
}

body:json {
  {
    "email":"{{Email}}",
    "password":"{{Password}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
